# TODO - Netlify deployment (Flutter Web)

- [ ] Add Netlify config (`netlify.toml`) to build and publish Flutter Web
- [ ] Verify Flutter web build output (`frontend/build/web/index.html`)
- [ ] Run SPA routing check (fallback to index.html)
- [ ] (If needed) Fix Flutter web build errors by adjusting `pubspec.yaml` / code
- [ ] Ensure backend API URL is correctly set via `API_BASE_URL`
- [ ] Commit + push

